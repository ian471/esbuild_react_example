# [ESBuild](https://esbuild.github.io/) [React](https://reactjs.org/) Example

Dev server:

    npm start

Build for production:

    npm run build

Built files are output to the `public/` directory.
